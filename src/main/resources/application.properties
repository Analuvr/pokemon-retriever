spring.application.name=pokemon-retriever

# Configuración para DB MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/pokemon_db?createDatabaseIfNotExist=true
spring.datasource.username=USUARIO
spring.datasource.password=CONTRASEÑA

# Configuraciones para el manejo de la DB
spring.jpa.hibernate.ddl-auto=update

# Configuración para ver las Queries que JPA realiza
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true